# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc
*.idx
*.ind
*.ilg
*.nav
*.snm
*.pos
*-SAVE-ERROR

# Generated document outputs
*.pdf

# LaTeX 排版線的 lualatex 工作目錄（KICKOFF-latex-pilot.md §4.1；轉出 body、
# 頁面 PNG、aux 皆可由 convert.py＋latexmk 重生）
handout/latex/build/

# Generated media pipeline outputs (frozen gen-0/gen-1, now archived under legacy/)
/legacy/artifacts/*
!/legacy/artifacts/scripts/
!/legacy/artifacts/slides/
!/legacy/artifacts/manim/
/legacy/artifacts/scripts/*
!/legacy/artifacts/scripts/*_final.md
/legacy/artifacts/slides/*
!/legacy/artifacts/slides/*.tex
/legacy/artifacts/manim/*
!/legacy/artifacts/manim/*/
/legacy/artifacts/manim/*/*
!/legacy/artifacts/manim/*/narration.md
!/legacy/artifacts/manim/manim_voiceover_experiment/README.md
!/legacy/artifacts/manim/manim_voiceover_experiment/voiceover_bookmark_demo.py

# Current gen-2 pipeline render outputs
/video/output/

# Manim CLI default output directory
media/

# Authoring R&D run outputs + handout render screenshots (regenerable; contain raw model responses)
authoring/**/out/
handout/html/_render/out/

# Local experiment scratch and run outputs
/experiments/

# Third-party reference scans (copyrighted; not for distribution)
stewart/

# Open problem bank local clones (third-party repos; see problem_banks/README.md)
problem_banks/*
!problem_banks/README.md

# Local raw voice inputs (frozen voice-clone pipeline, archived under legacy/)
/legacy/inputs/voice/*.wav
/legacy/inputs/voice/*.mp3
/legacy/inputs/voice/*.m4a
/legacy/inputs/voice/*.flac

# Python caches
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
.ipynb_checkpoints/
.python-version

# Local environments and vendored dependencies
.venv/
.venv_test/
.deps/
.deps_f5/
.deps_voiceover/

# Secrets / API keys (never commit — Gemini etc. read the key from the environment)
.env
.env.*
*.key

# Local caches and temp files
.cache/
.pip-cache/
.pip-cache-f5/
.tmp/
*.tmp
*.bak
*.orig
# Ad-hoc PDF-page previews (pdftoppm, pdf2image, etc.)
tmp_*.png
tmp_*.jpg
tmp_*.pdf

# Editor and IDE metadata
.vscode/
.idea/
*.code-workspace

# Claude Code: ignore personal/volatile state, but track shared project assets
# (agents, commands, skills, output styles, shared settings). New shared assets
# under a whitelisted dir are tracked automatically — no `git add -f` needed.
# Ignored: settings.local.json (personal), worktrees/ (git work-trees), launch.json
# (ad-hoc local QA), scheduled_tasks.lock, and anything else under .claude/.
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/skills/
!.claude/output-styles/
!.claude/settings.json

# OS metadata
Thumbs.db
Desktop.ini
.DS_Store

# 圖匯出的建置中間物（handout/latex/chapters/<ch>/figs/）——
# export_figs.mjs 產的向量 PDF、wrapper 頁與 figures.json，隨時可由 standalone 重生，
# 不進版控（DIALECT-ch01.md §3／DIALECT-ch02.md §3 已如此記載，但先前只有 *.pdf 被
# 全域規則擋住，_wrap/*.html 與 figures.json 一直掛在 git status 的未追蹤區）。
handout/latex/chapters/*/figs/

# LaTeX 成品夾（handout/latex/dist/<ch>/＝一 tex 一 pdf，皆進版控）——
# 成品 PDF 是 *.pdf 全域忽略的例外；編譯殘渣（若有人用非 latexmk 流程）不進
!handout/latex/dist/**/*.pdf
handout/latex/dist/**/*.aux
handout/latex/dist/**/*.log
handout/latex/dist/**/*.fls
handout/latex/dist/**/*.fdb_latexmk
handout/latex/dist/**/*.synctex.gz
handout/latex/dist/**/*.out
handout/latex/dist/**/*.toc

# 自驗／審閱的本機產出（皆可重生，不進版控）——
# `/handout/_render/`＝從錯誤 CWD 跑 shot.mjs 留下的散落輸出（工具的正規位置是
# 版控中的 handout/html/_render/，只有 .mjs 兩支進版控，輸出一律不進）。
# `/tmp/`＝video 線的審閱素材（contact sheet／keyframes 等）。兩者路徑前置 `/`
# 錨在 repo 根，才不會誤傷 handout/html/_render/ 底下的工具檔。
/handout/_render/
/tmp/
