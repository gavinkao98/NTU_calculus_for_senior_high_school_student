# §8.5 Strategy for Integration — Codex ⑤ audit record (raw JSON gitignored)

2026-07-26. gpt-5.6-terra／max, read-only, output-schema, post-generation batch.

## Round 1 — 1 blocking, 0 advisory

- **[8.5-B1｜direction-conformance｜BLOCKING → adjudicated as a BRIEF defect]** brief-8-5's
  Ex 8.24 line labeled \(u=\sqrt x\) a "step 2" substitution; the draft calls it "step 4's
  advice". The draft is semantically correct: Strategy 8.6 step 2 = *visible* \(du\) present
  (which \(e^{\sqrt x}\) lacks); step 4 = substitute to expose structure. **Adjudication
  (delegated 拍板): the defect was in the brief, not the draft** — brief-8-5 amended to step 4
  with an inline adjudication note; fragment unchanged. Recorded so the ⑤ contract and the draft
  agree going forward.

Auditor-verified clean: Ex 8.23–8.26 recomputed; Strategy 8.6 four steps; table of standard forms
(all entries established, provenance correct; scaled arcsin verified inline via Ex 3.14 + chain
rule); the elementary-antiderivative paragraph in the ③ B-02 scope-statement form (no third
credit); the four-integral opener's dispatch claims.

## Round 2 (scoped regression) — see `ch08_chapter-sweep-audit.md` §regression

**Status: §8.5 ⑤ CLOSED — 0 blocking after adjudication + regression.**
